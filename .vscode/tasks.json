{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build",
            "command": "dotnet run --project _build --no-launch-profile -- build",
            "type": "shell",
            "options": {
                "cwd": "${workspaceRoot}"
            },
            "group": "build",
            "presentation": {
                "echo": false,
                "focus": true
            },
            "problemMatcher": "$msCompile"
        }
    ]
}